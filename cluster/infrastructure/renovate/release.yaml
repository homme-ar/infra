apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: renovate
  namespace: renovate
spec:
  interval: 30m
  chart:
    spec:
      chart: renovate
      version: "46.*"
      sourceRef:
        kind: HelmRepository
        name: renovate
        namespace: flux-system
      interval: 12h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    cronjob:
      schedule: "17 * * * *"
      concurrencyPolicy: Forbid
      successfulJobsHistoryLimit: 3
      failedJobsHistoryLimit: 3
      jobBackoffLimit: 1
    renovate:
      configIsJavaScript: true
      config: |
        module.exports = {
          platform: 'github',
          repositories: ['homme-ar/infra'],
          onboarding: false,
          requireConfig: 'ignored',
          prHourlyLimit: 5,
          prConcurrentLimit: 10,
          labels: ['dependencies'],
          // Default fileMatch of these managers only covers files named
          // helmrelease.yaml / kubernetes manifests in specific dirs; this repo
          // keeps Flux HelmReleases in release.yaml files under cluster/.
          flux: {
            fileMatch: ['cluster/.*\\.yaml$'],
          },
          kubernetes: {
            fileMatch: ['cluster/.*\\.yaml$'],
          },
          // One PR per project (leaf directory under cluster/), containing all
          // of its dependency updates. When adding a new app/component, add a
          // matching rule here.
          packageRules: [
            { matchPaths: ['cluster/apps/iot/esphome/**'], groupName: 'esphome', groupSlug: 'esphome' },
            { matchPaths: ['cluster/apps/iot/home-assistant/**'], groupName: 'home-assistant', groupSlug: 'home-assistant' },
            { matchPaths: ['cluster/apps/iot/mosquitto/**'], groupName: 'mosquitto', groupSlug: 'mosquitto' },
            { matchPaths: ['cluster/apps/iot/zigbee2mqtt/**'], groupName: 'zigbee2mqtt', groupSlug: 'zigbee2mqtt' },
            { matchPaths: ['cluster/apps/media/bazarr/**'], groupName: 'bazarr', groupSlug: 'bazarr' },
            { matchPaths: ['cluster/apps/media/jellyfin/**'], groupName: 'jellyfin', groupSlug: 'jellyfin' },
            { matchPaths: ['cluster/apps/media/jellyseerr/**'], groupName: 'jellyseerr', groupSlug: 'jellyseerr' },
            { matchPaths: ['cluster/apps/media/lidarr/**'], groupName: 'lidarr', groupSlug: 'lidarr' },
            { matchPaths: ['cluster/apps/media/prowlarr/**'], groupName: 'prowlarr', groupSlug: 'prowlarr' },
            { matchPaths: ['cluster/apps/media/radarr/**'], groupName: 'radarr', groupSlug: 'radarr' },
            { matchPaths: ['cluster/apps/media/sabnzbd/**'], groupName: 'sabnzbd', groupSlug: 'sabnzbd' },
            { matchPaths: ['cluster/apps/media/sonarr/**'], groupName: 'sonarr', groupSlug: 'sonarr' },
            { matchPaths: ['cluster/apps/platform/authelia/**'], groupName: 'authelia', groupSlug: 'authelia' },
            { matchPaths: ['cluster/apps/platform/headlamp/**'], groupName: 'headlamp', groupSlug: 'headlamp' },
            { matchPaths: ['cluster/apps/platform/index/**'], groupName: 'index', groupSlug: 'index' },
            { matchPaths: ['cluster/apps/platform/ittools/**'], groupName: 'ittools', groupSlug: 'ittools' },
            { matchPaths: ['cluster/apps/platform/librespeed/**'], groupName: 'librespeed', groupSlug: 'librespeed' },
            { matchPaths: ['cluster/apps/platform/n8n/**'], groupName: 'n8n', groupSlug: 'n8n' },
            { matchPaths: ['cluster/apps/platform/ntfy/**'], groupName: 'ntfy', groupSlug: 'ntfy' },
            { matchPaths: ['cluster/apps/platform/postgres/**'], groupName: 'postgres', groupSlug: 'postgres' },
            { matchPaths: ['cluster/apps/platform/vaultwarden/**'], groupName: 'vaultwarden', groupSlug: 'vaultwarden' },
            { matchPaths: ['cluster/apps/platform/waha/**'], groupName: 'waha', groupSlug: 'waha' },
            { matchPaths: ['cluster/flux-system/**'], groupName: 'fluxcd', groupSlug: 'fluxcd' },
            { matchPaths: ['cluster/infrastructure/etcd-backup/**'], groupName: 'etcd-backup', groupSlug: 'etcd-backup' },
            { matchPaths: ['cluster/infrastructure/networking/cilium/**'], groupName: 'cilium', groupSlug: 'cilium' },
            { matchPaths: ['cluster/infrastructure/networking/gateway/**'], groupName: 'gateway', groupSlug: 'gateway' },
            { matchPaths: ['cluster/infrastructure/networking/wireguard/**'], groupName: 'wireguard', groupSlug: 'wireguard' },
            { matchPaths: ['cluster/infrastructure/observability/adguard-exporter/**'], groupName: 'adguard-exporter', groupSlug: 'adguard-exporter' },
            { matchPaths: ['cluster/infrastructure/observability/dashboards/**'], groupName: 'dashboards', groupSlug: 'dashboards' },
            { matchPaths: ['cluster/infrastructure/observability/kube-prometheus-stack/**'], groupName: 'kube-prometheus-stack', groupSlug: 'kube-prometheus-stack' },
            { matchPaths: ['cluster/infrastructure/observability/metrics-server/**'], groupName: 'metrics-server', groupSlug: 'metrics-server' },
            { matchPaths: ['cluster/infrastructure/observability/node-feature-discovery/**'], groupName: 'node-feature-discovery', groupSlug: 'node-feature-discovery' },
            { matchPaths: ['cluster/infrastructure/observability/servicemonitors/**'], groupName: 'servicemonitors', groupSlug: 'servicemonitors' },
            { matchPaths: ['cluster/infrastructure/renovate/**'], groupName: 'renovate', groupSlug: 'renovate' },
            { matchPaths: ['cluster/infrastructure/security/cert-manager-config/**'], groupName: 'cert-manager-config', groupSlug: 'cert-manager-config' },
            { matchPaths: ['cluster/infrastructure/security/cert-manager/**'], groupName: 'cert-manager', groupSlug: 'cert-manager' },
            { matchPaths: ['cluster/infrastructure/security/kubelet-serving-cert-approver/**'], groupName: 'kubelet-serving-cert-approver', groupSlug: 'kubelet-serving-cert-approver' },
            { matchPaths: ['cluster/infrastructure/storage/cloudnativepg/**'], groupName: 'cloudnativepg', groupSlug: 'cloudnativepg' },
            { matchPaths: ['cluster/infrastructure/storage/longhorn/**'], groupName: 'longhorn', groupSlug: 'longhorn' },
            { matchPaths: ['cluster/infrastructure/storage/versitygw/**'], groupName: 'versitygw', groupSlug: 'versitygw' },
          ],
        };
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
    securityContext:
      runAsNonRoot: true
      runAsUser: 1000
    # All keys of this secret are injected as environment variables (RENOVATE_TOKEN).
    existingSecret: renovate-github-token
    env:
      LOG_LEVEL: info
    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: "1"
        memory: 1Gi
