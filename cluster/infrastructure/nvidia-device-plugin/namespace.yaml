apiVersion: v1
kind: Namespace
metadata:
  name: nvidia-device-plugin
  labels:
    # The device plugin DaemonSet mounts hostPaths (kubelet device-plugins dir,
    # CDI root), which the default baseline PodSecurity policy forbids.
    pod-security.kubernetes.io/enforce: privileged
