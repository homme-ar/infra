apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: versitygw-data
  namespace: backup
  labels:
    app.kubernetes.io/name: versitygw
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: versitygw-nfs
  resources:
    requests:
      storage: 1Pi
