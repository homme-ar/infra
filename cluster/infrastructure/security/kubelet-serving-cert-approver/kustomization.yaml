apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Pinned to v0.11.0 (latest stable release).
  # The manifest already includes the `kubelet-serving-cert-approver` namespace.
  - https://raw.githubusercontent.com/alex1989hu/kubelet-serving-cert-approver/v0.11.0/deploy/standalone-install.yaml
