apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${CLUSTER_DOMAIN_SLUG}-tls
  namespace: kube-system
spec:
  secretName: ${CLUSTER_DOMAIN_SLUG}-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ${CLUSTER_DOMAIN}
    - "*.${CLUSTER_DOMAIN}"
