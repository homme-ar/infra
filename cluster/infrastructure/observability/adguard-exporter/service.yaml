apiVersion: v1
kind: Service
metadata:
  name: adguard-exporter
  namespace: observability
  labels:
    app.kubernetes.io/name: adguard-exporter
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: adguard-exporter
  ports:
    - name: metrics
      port: 9618
      targetPort: metrics
