apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homme-ar-tls
  namespace: kube-system
spec:
  secretName: homme-ar-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - homme.ar
    - "*.homme.ar"
