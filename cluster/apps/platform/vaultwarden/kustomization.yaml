apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: vaultwarden
resources:
  - namespace.yaml
  - pvc-data.yaml
  # MIGRATION resources — remove after migration Job completes successfully
  - pv-nfs-source.yaml
  - pvc-nfs-source.yaml
  - migration-job.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml
  - httproute.yaml
