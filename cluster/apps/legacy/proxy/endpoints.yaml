apiVersion: v1
kind: Endpoints
metadata:
  name: legacy-cluster-ingress
  namespace: legacy
subsets:
  - addresses:
      - ip: ${LEGACY_INGRESS_IP}
    ports:
      - name: http
        port: 80
      - name: https
        port: 443
