apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media
  namespace: sabnzbd
  labels:
    app.kubernetes.io/name: sabnzbd
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: sabnzbd-nas
  resources:
    requests:
      storage: 1Pi
