apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media
  namespace: lidarr
  labels:
    app.kubernetes.io/name: lidarr
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: lidarr-nas
  resources:
    requests:
      storage: 1Pi
