apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media
  namespace: prowlarr
  labels:
    app.kubernetes.io/name: prowlarr
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: prowlarr-nas
  resources:
    requests:
      storage: 1Pi
