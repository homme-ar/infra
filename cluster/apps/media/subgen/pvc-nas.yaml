apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nas-media
  namespace: subgen
  labels:
    app.kubernetes.io/name: subgen
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ""
  volumeName: subgen-nas
  resources:
    requests:
      storage: 1Pi
