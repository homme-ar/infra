apiVersion: v1
kind: Service
metadata:
  name: subgen
  namespace: subgen
  labels:
    app.kubernetes.io/name: subgen
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 9000
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: subgen
