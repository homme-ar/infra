apiVersion: v1
kind: Service
metadata:
  name: ha-mcp
  namespace: ha-mcp
  labels:
    app.kubernetes.io/name: ha-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ha-mcp
